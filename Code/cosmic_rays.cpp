#include <bits/stdc++.h>
using namespace std;
void solve(){
    int n;cin>>n;
    stack<pair<int,int>> st;
    vector<int> v(n);
    while(n--){
        pair<int,int> p;
        cin>>p.first>>p.second;
        if(st.empty()){
            st.push(p);
            v[i]=p.first;
        }

        while(!st.empty()){

        }
    }
}
int main(){
    int t;cin>>t;
    while(t--){
        solve();
    }
}